#pragma once
#include<ctype.h>
#include <stdlib.h>
#include <string.h>

const char* get_substr_with_len(const char* source, int beg, int end);
const char* get_substr_fromto(const char* source, int beg, int end);

